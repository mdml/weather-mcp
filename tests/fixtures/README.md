# Test fixtures

Recorded Open-Meteo responses live here (Phase 1) so parsing/aggregation runs offline and
deterministic against real payloads — the bulk of `cargo nextest run` (DEVELOPMENT.md §
Testing strategy).

Empty in Phase 0: there is no weather logic yet. The forecast + archive JSON fixtures land
alongside the three real tools.
